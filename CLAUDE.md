# 꽁지맵

법원경매·온비드 물건을 공시가격 기준으로 걸러 지도에 올리는 MVP. 바닐라 JS + Node, 빌드 도구 없음.

---

## 이 컴퓨터에 있는 크롤러 폴더는 열지 않는다

`~/Documents` 안에 크롤러처럼 보이는 폴더가 여러 개 남아 있다. **전부 마이그레이션
이전의 옛날 파일이고, 지금 꽁지맵이 부르는 크롤러가 아니다.**

- `auction_crawler_runtime/`
- `경매물건 크롤링/`
- `경매물건 크롤링 복사본/`
- `경매물건 크롤링.zip`

읽지 말고, 참고하지 말고, 여기서 본 것을 근거로 판단하지 않는다. 이름이 그럴듯해서
"지금 쓰는 코드"로 착각하기 쉬운데, 실제로 돌고 있는 크롤러는
`COURT_AUCTION_API_URL`이 가리키는 **원격 API**다. 로컬 폴더가 아니다.

크롤러가 무엇을 주는지 알아야 하면 `lib/court-detail.mjs`의 `normalizeDetail()`을 본다.
화면이 실제로 쓰는 필드가 거기 전부 적혀 있다.

**세션의 작업 범위는 `꽁지맵 개발/` 폴더 안이다.** 폴더 밖을 볼 일이 생기면 먼저 묻는다.

---

## 세션 작업 규칙

이 프로젝트는 Claude Code 세션 여러 개가 돌아간다. 아래는 그래서 필요한 규칙이다.

1. **작업을 시작하면 먼저 `git status`와 `git log --oneline -5`를 본다.**
   다른 세션이 방금 만든 변경이 떠 있을 수 있다. 남의 변경 위에 덮어쓰지 않는다.

2. **작업이 끝나면 반드시 커밋하고 나간다. 단, 커밋까지다.**
   커밋 안 된 변경을 남긴 채 세션을 떠나면, 다음 세션이 그게 누구 것인지 알 수 없다.
   과거에 이것 때문에 8개 파일 420줄이 주인 없는 덩어리로 남은 적이 있다.

3. **`git push`는 매번 사용자에게 물어보고 허락을 받는다. 예외 없다.**
   push는 곧 배포다(Vercel이 main을 자동으로 올린다). 커밋은 되돌리기 쉽지만
   배포된 화면은 사용자가 이미 쓰고 있다. "고쳤으니 당연히 올린다"고 넘겨짚지 않는다.
   - 이전 턴에서 허락받았다고 다음 턴까지 이어지지 않는다. 매번 다시 묻는다.
   - "확인해봐" / "검증해봐" / "고쳐봐" / "지워도 되는지 봐봐"는 push 허락이 아니다.
     "배포해" / "올려"처럼 명시적으로 말했을 때만 한다.
   - 커밋해 두고 "푸시할까요?"라고 묻는 것이 기본 동작이다.

4. **한 세션은 한 주제만 다룬다.** 주제가 바뀌면 새 세션을 연다.

5. **확정된 결정은 `docs/DECISIONS.md`에 한 줄 남긴다.** 제안 단계는 적지 않는다.
   적히지 않은 것은 자동으로 "아직 미확정"이다.

6. **큰 리팩터링·구조 변경은 시작 전에 사용자에게 확인한다.**
   다른 세션이 같은 파일을 작업 중일 수 있다.

---

## 세션별 작업 폴더 — 선택 사항, 평소엔 쓰지 않는다

**기본값은 원본 폴더에서 그냥 작업하는 것이다.** 위의 규칙 1~5만 지키면 대부분의 충돌은
일어나지 않는다. 실제로 났던 사고 두 건(주인 없는 420줄, `.tag.good` CSS 회귀)도 폴더를
나눴다고 막히는 종류가 아니었다.

아래는 **두 세션이 같은 파일을 동시에 고치고 있다는 게 확인된 경우에만** 꺼내 쓴다.
사용자는 CLI를 쓰지 않는다. **필요하다고 판단되면 세션이 직접 실행하고, 사용자에게는
"작업 폴더를 나눌까?"라고 물어본다.**

```bash
./scripts/새작업.sh 디자인     # ../꽁지맵-디자인 폴더 + work/디자인 브랜치 생성
./scripts/작업목록.sh          # 지금 열려 있는 작업 폴더와 저장 상태
./scripts/작업끝.sh            # (작업 폴더 안에서) main 에 합치고 폴더 정리
```

- 만들어진 폴더에서 작업하려면 세션의 작업 디렉터리를 그쪽으로 옮겨야 한다.
  사용자 손이 한 번 필요하므로, 그만한 값어치가 있을 때만 제안한다.
- `.env`는 git이 관리하지 않아 새 폴더에 생기지 않으므로 `새작업.sh`가 원본을 심볼릭 링크로
  이어준다. 복사하지 않는다 — 키를 한 곳에서만 고치기 위해서다.
- `작업끝.sh`는 저장 안 된 변경이 있으면 멈추고, 충돌이 나면 `merge --abort`로 되돌린 뒤
  사람에게 넘긴다. 원본을 어중간한 상태로 두지 않는다.

---

## 실행

```bash
npm run dev          # dev-server.mjs, http://localhost:4173
```

Claude Code에서는 `.claude/launch.json`의 `ggongji` 설정을 쓴다 (법원 API 스냅샷 모드).
서버를 Bash로 직접 띄우지 말고 preview 도구를 쓴다.

키는 `.env`에 있다. `.env.example` 참고. **키 값을 커밋하거나 대화에 출력하지 않는다.**

---

## 구조와 단일 진입점

같은 일을 하는 코드가 두 곳에 생기면 세션끼리 반드시 어긋난다. 아래는 "여기서만 고친다"는 지점이다.

| 관심사 | 단 하나의 파일 |
|---|---|
| 물건 종별 분류 (대분류 5 / 세분류 23, 매각 형태) | `categories.js` — 서버와 브라우저가 같은 파일을 읽는다 |
| 좌표 추정 테이블, 뷰포트 경계 계산 | `lib/geo.mjs` |
| 외부 데이터 → 지도용 물건 정규화 | `lib/normalize.mjs` |

- 종별을 판정하는 새 규칙이 필요하면 `categories.js`에 넣는다. `app.js`나 어댑터에서 정규식으로 다시 훑지 않는다.
- 소스 어댑터(`lib/court.mjs`, `lib/onbid.mjs`)가 이미 분류해 둔 물건은 `normalize.mjs`에서 다시 분류하지 않는다.
  주소 대괄호가 잘려나간 뒤라 목록구분과 지목이 통째로 사라진다.

자세한 배경은 `README.md`의 "물건 종별 분류" 절에 있다.

---

## 동시 편집 주의 파일

아래 두 파일은 어떤 주제로 작업해도 결국 들어오게 된다. 세션 간 충돌이 가장 잦은 곳이다.

- `app.js` (약 2,600줄)
- `styles.css` (약 2,800줄)

이 두 파일을 편집하기 전에는 `git status`로 다른 세션의 미저장 변경이 없는지 확인한다.
`styles.css`는 같은 셀렉터가 상단과 미디어쿼리(`@media (max-width: 720px)`) 안에 각각 있으므로,
규칙을 추가할 때 **선택자 목록 중간에 끼워 넣지 않도록** 주의한다.

---

## 커밋

- 메시지는 한국어. 제목은 **무엇이 문제였고 원인이 무엇이었는지**를 쓴다. 파일 목록이 아니라.
  - 좋음: `서울 밖 지역이 "물건 없음"으로 뜨던 문제: 뷰포트 캐시가 지역을 구분하지 않았다`
  - 나쁨: `app.js 수정`
- 관계없는 변경을 한 커밋에 섞지 않는다. 주제별로 나눈다.
- 커밋 전에 최소한 문법 검사는 한다: `node --check app.js`

---

## 코드 스타일

- 주석은 한국어. **무엇을 하는지가 아니라 왜 그렇게 했는지**를 쓴다.
  특히 "이렇게 안 하면 무엇이 깨지는지"를 남긴다. 기존 코드가 그 방식으로 쓰여 있다.
- 빌드 도구가 없다. 브라우저가 바로 읽는 코드다. 트랜스파일이 필요한 문법을 쓰지 않는다.
- `index.html`의 `?v=` 캐시 버스터는 `app.js` / `styles.css` / `categories.js`를 고칠 때 함께 올린다.
